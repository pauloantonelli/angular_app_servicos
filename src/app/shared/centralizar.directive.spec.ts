import { CentralizarDirective } from './centralizar.directive';

describe('CentralizarDirective', () => {
  it('should create an instance', () => {
    const directive = new CentralizarDirective();
    expect(directive).toBeTruthy();
  });
});
